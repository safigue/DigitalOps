import { LyncPresenceDirective } from './lync-presence.directive';

describe('LyncPresenceDirective', () => {
  it('should create an instance', () => {
    const directive = new LyncPresenceDirective();
    expect(directive).toBeTruthy();
  });
});
